<%= div_for bark do %>
  <%= link_to bark.user.username, bark.user %>
  barked out
  <%= render bark.content %>
  <%= link_to time_ago_in_words(bark.created_at), bark %>
  ago
  <%= button_to "Delete Bark", bark_path(bark), method: :delete %>
<% end %>

